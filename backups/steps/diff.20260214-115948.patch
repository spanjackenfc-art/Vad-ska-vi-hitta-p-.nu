diff --git a/app/page.tsx b/app/page.tsx
index d32d989..7a0399b 100644
--- a/app/page.tsx
+++ b/app/page.tsx
@@ -1,5 +1,6 @@
 import BottomFilterBar from "./_components/BottomFilterBar";
 import { SWEDISH_CITIES } from "@/lib/swedishCities";
+import NewsletterPopup from "./_components/NewsletterPopup";
 
 import DescriptionToggle from "./_components/DescriptionToggle";
 import Hero from "./_components/Hero";
@@ -771,9 +772,91 @@ return (
 
       {/* Content */}
 
-      <div className="mx-auto max-w-6xl px-6 py-8">
+      <div className="mx-auto max-w-6xl px-6 py-8 lg:grid lg:grid-cols-[1fr_280px] lg:gap-8">
         <div className="grid gap-6">
-          
+
+{/* Right rail (does not affect main grid) */}
+<div className="hidden lg:block">
+  <div className="sticky top-24">
+    <div className="flex items-baseline justify-between">
+      <h3 className="text-sm font-semibold">Tips</h3>
+      <span className="text-xs text-slate-500">Utvalda</span>
+    </div>
+
+    <div className="mt-3 grid gap-3" role="list">
+      {(() => {
+        const WANT = [
+          "Evigt Edvin",
+          "Pappa kapsyl",
+          "Trad On The Prom",
+          "Bygdespelet Höga Kusten",
+          "Djungelboken the Musical",
+        ];
+
+        const norm = (s: any) =>
+          String(s || "")
+            .toLowerCase()
+            .replace(/\s+/g, " ")
+            .replace(/["\-–—:;,.!()\[\]]/g, "")
+            .trim();
+
+        const wantSet = new Set(WANT.map(norm));
+
+        const pool = Array.isArray(eventsForList) ? eventsForList : [];
+        const picked: any[] = [];
+        for (const e of pool) {
+          const t = norm(e?.title);
+          if (!t) continue;
+          if (![...wantSet].some(w => t.includes(w))) continue;
+          if (picked.some(x => String(x?.id) === String(e?.id))) continue;
+          picked.push(e);
+          if (picked.length >= 5) break;
+        }
+
+        if (picked.length < 5) {
+          for (const e of pool) {
+            if (picked.length >= 5) break;
+            if (picked.some(x => String(x?.id) === String(e?.id))) continue;
+            picked.push(e);
+          }
+        }
+
+        return picked.length ? (
+          picked.map((e: any) => (
+            <div role="listitem" key={"rail-" + String(e.id || "") } className="list-none">
+              <a
+                href={String(e.ticket_url || e.source_url || "#")}
+                target="_blank"
+                rel="noreferrer"
+                className="group flex gap-2 rounded-2xl bg-white p-2 ring-1 ring-slate-200 shadow-sm hover:bg-slate-50"
+              >
+                <div className="relative h-14 w-14 shrink-0 overflow-hidden rounded-xl bg-slate-200">
+                  <img
+                    src={String(bestImageSrc(e || { category: "övrigt" }))}
+                    alt=""
+                    className="h-full w-full object-cover"
+                    loading="lazy"
+                  />
+                </div>
+                <div className="min-w-0">
+                  <div className="mt-0.5 text-[13px] font-semibold leading-snug text-slate-900 line-clamp-2">
+                    {String(e.title || "")}
+                  </div>
+                  <div className="mt-0.5 text-xs text-slate-600 line-clamp-1">
+                    {String(e.venue_name || e.city || "")}
+                  </div>
+                </div>
+              </a>
+            </div>
+          ))
+        ) : (
+          <div className="text-xs text-slate-600">Inga av de valda tipsen finns i listan just nu.</div>
+        );
+      })()}
+    </div>
+  </div>
+</div>
+
           {/* List */}
           <section className="bg-transparent" suppressHydrationWarning>
             <div id="list" />
@@ -782,7 +865,7 @@ return (
               <div>
                 <div id="list" />
                 <h2 className="text-sm font-semibold">Kommande events</h2>
-                <p className="mt-1 text-xs text-slate-600">{eventsForList.length} visade (max 50)</p>
+                <p className="mt-0.5 text-xs text-slate-600">{eventsForList.length} visade (max 50)</p>
 
     <div className="mt-3 inline-flex items-center gap-2 rounded-2xl bg-white p-1 ring-1 ring-slate-200">
       <a
@@ -840,7 +923,7 @@ return (
                 </div>
                 <div className="mt-3 grid gap-5 sm:grid-cols-2 lg:grid-cols-3" role="list">
                   {topSponsored.map((x: any, i: number) => (
-                    <div role="listitem" key={String(x.ev.id) + "-" + i} className="list-none">
+                    <div role="listitem" key={String(x.ev.id)} className="list-none">
                       <a
                         href={String(x.ev.ticket_url || x.ev.source_url || "#")}
                         target="_blank"
@@ -857,7 +940,7 @@ return (
                           <div className="text-xs font-semibold uppercase tracking-wide text-slate-500">
                             Sponsrat
                           </div>
-                          <div className="mt-1 text-xs text-slate-500">
+                          <div className="mt-0.5 text-xs text-slate-500">
                             {String(x.sp.package || "").toUpperCase()} {String(x.sp.package || "") === "P4" ? "Partner" : "Topplista"}
                           </div>
                           <div className="mt-1 text-base font-semibold tracking-tight text-slate-900">
@@ -1193,6 +1276,7 @@ if (isGuidedTour && (!e.image_url || String(e.image_url).trim() === "")) {
       {/* Bottom filters */}
 <BottomFilterBar cities={cities} monthOptions={monthOptions} />
 
-    </main>
+      <NewsletterPopup />
+</main>
   );
 }
